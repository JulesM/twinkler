{% extends 'TkUserBundle::frame.html.twig' %}

{% block stylesheets %}
 {{ parent() }}
 <link href="{{ asset('css/navbar.css') }}" rel="stylesheet" type="text/css">
{% endblock %}

{% set group = app.user.currentMember.tgroup %}

{% block navbartitle %}
	<h1>{{ group.name }}</h1>
{% endblock %}

{% block header %}
<div class="container">
	<div class="row" style="height: 95px;">
		<div id="add-group" class="span3">
		  <a href="{{ path('tk_group_new') }}"><img src="{{ asset('img/Frame/add-group.png') }}" class="img-circle img-shadow"></a>
		</div>
		<div id="group" class="span6">

				{% for member in group.members %}
					{% if member.user %}
						<div class="member">
							<img src="{{ asset(member.user.picture.path) }}" class="img-circle" width="60px" 	alt="{{ member.name }}" style="position: relative">
							<img src="{{ asset('img/Frame/pic-overlay.png') }}" class="img-circle pic-overlay" width="60px" 	alt="{{ member.name }}">
							<p>{{ member.name }}</p>
						</div> 
					{% else %}
						<div class="member">
							<img src="{{ asset('uploads/profile-pictures/profile-pic.png') }}" class="img-circle" width="60px" 	alt="{{ member.name }}" style="position: relative">
							<img src="{{ asset('img/Frame/pic-overlay.png') }}" class="img-circle pic-overlay" width="60px" 	alt="{{ member.name }}">
							<p>{{ member.name }}</p>
						</div>
					{% endif %}
				{% endfor %}

		</div>
		<div id="toolbar" class="span3">
			<div style="position: absolute; bottom: 0;">
				<a href="{{ path('tk_group_add_members') }}"><img src="{{ asset('img/Frame/add-member.png') }}" class="img-circle img-shadow"></a>
				<div class="btn-group" style="display: inline-block">
					<a class="btn btn-link dropdown-toggle" data-toggle="dropdown" href="#"><img src="{{ asset('img/Frame/add-invite.png') }}" class="img-circle img-shadow"></a>
					<ul class="dropdown-menu">
						<!-- dropdown menu links -->
						{% render(controller('TkGroupBundle:Default:sendInvitationEmail')) %}
						<a class="btn btn-primary" href="https://www.facebook.com/dialog/send?app_id=130641483808717&name=Click%20here%20to%20join%20my%20group%20on%20Twinkler&link=http://www.twinkler.co/invitation/{{group.id}}/{{group.invitationToken}}&redirect_uri=http://www.twinkler.co/group/settings">Send Facebook Message</a>
					</ul>
				</div>
				<a href="{{ path('tk_group_homepage') }}"><img src="{{ asset('img/Frame/settings.png') }}" class="img-circle img-shadow"></a>
			</div>
		</div>		
	</div>
</div>

<div class="selection-menu">
	<div class="container">
		<div class="row">
			<div class="span12">
				<div class="span2 offset4" style="text-align: center">
					<a href="{{ path('tk_expense_homepage') }}">Expenses</a>
				</div>
				<div class="span2" style="text-align: center">
					<a href="{{ path('tk_list_homepage') }}">Lists</a>
				</div>
			</div>
		</div>
	</div>
</div>

{% endblock %}